---
const tiers = [
  {
    name: 'Free',
    price: '$0',
    period: '',
    desc: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit.',
    features: [
      'Up to 2 cards',
      'Lorem ipsum dolor sit amet',
      'Consectetur adipiscing elit',
      'Sed do eiusmod tempor',
    ],
    featured: false,
    cta: 'Get started',
  },
  {
    name: 'Pro',
    price: '$4',
    period: '/ month',
    desc: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor.',
    features: [
      'Unlimited cards',
      'Best card nearby (map)',
      'Lorem ipsum dolor sit amet',
      'Consectetur adipiscing elit',
      'Sed do eiusmod tempor incididunt',
    ],
    featured: true,
    cta: 'Start Pro',
  },
] as const;
---
<section id="pricing" class="relative mx-auto max-w-[920px] px-6 pt-[60px] pb-[100px]">
  <div class="text-center mb-14">
    <h2
      class="font-display font-semibold text-4xl md:text-6xl leading-[1.02] m-0 text-zinc-950"
    >
      Pricing.
    </h2>
    <p class="text-[17px] text-zinc-500 mt-4 max-w-[540px] mx-auto leading-[1.5]">
      Free for two cards. A few dollars a month for the rest.
    </p>
  </div>

  <div style="display:grid;grid-template-columns:1fr 1fr;gap:18px">
    {tiers.map(({ name, price, period, desc, features, featured, cta }) => (
      <div class={`${featured ? 'card-featured' : 'card'} flex flex-col relative p-9`}>
        {featured && (
          <div class="absolute top-4 right-4 px-[11px] py-1 rounded-full bg-zinc-950 text-white text-[11.5px] font-semibold">
            Popular
          </div>
        )}
        <div class="text-[15px] font-semibold text-zinc-500">{name}</div>
        <div class="flex items-baseline gap-1 mt-2 mb-1.5">
          <span class="text-[56px] font-semibold leading-none text-zinc-950 tabular-nums">{price}</span>
          {period && <span class="text-[17px] text-zinc-500">{period}</span>}
        </div>
        <p class="text-[14px] text-zinc-500 m-0 mb-6 leading-[1.55]">{desc}</p>
        <ul class="list-none p-0 m-0 flex flex-col gap-2.5 flex-1">
          {features.map((f) => (
            <li class="flex items-center gap-2.5 text-[14.5px] text-zinc-800">
              <svg width={14} height={14} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M5 12l5 5L20 7" />
              </svg>
              {f}
            </li>
          ))}
        </ul>
        <a
          href="#download"
          class={`no-underline inline-flex items-center justify-center gap-1.5 rounded-full font-semibold mt-7 px-5 py-3 text-[14.5px] ${featured ? 'bg-zinc-950 text-white border-0' : 'bg-transparent text-zinc-950 border border-line'}`}
        >
          {cta}
        </a>
      </div>
    ))}
  </div>
</section>
