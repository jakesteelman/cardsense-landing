# Pre-Deploy Checklist

## Cloudflare Environment Variables

- [ ] Add `RESEND_API_KEY` as a **Secret** in Cloudflare Pages dashboard
  - Pages → cardsense-landing → Settings → Environment variables → Add secret
  - Must be set for both **Production** and **Preview** environments
